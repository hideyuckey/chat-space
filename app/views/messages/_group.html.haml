- current_user.groups.each do |group|
  = link_to "group_messages_path(group)", class: "group-list__link" do
    .group-list
      .group-list__name
        = group.name
      .group-list__new-chat
        メッセージはまだありません